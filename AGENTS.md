# Repository Rules

## Specs

- Design/spec files under `docs/superpowers/specs/` must remain gitignored.
- Do not commit spec files to the repository.
- If a spec file is accidentally force-added or tracked, remove it from the git index and keep the local file only.
